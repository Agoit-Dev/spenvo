package com.agoitdev.spenvo.designsystem.components

/** Pre-resolved strings for [AvatarMenu] — the caller supplies `stringResource(...)` output. */
data class AvatarMenuTextos(
    val estado: String?,
    val cuenta: String,
    val ajustes: String,
)
