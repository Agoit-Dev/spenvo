{
  "name": "spenvo-rules-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "firebase --config ../firebase.json --project spenvo-dev emulators:exec --only firestore,storage \"node --test --test-force-exit rules.test.mjs storage-rules.test.mjs\"",
    "emulators": "firebase emulators:start --only firestore,storage"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.2",
    "firebase-tools": "^15.28.1"
  },
  "overrides": {
    "uuid": ">=11.1.1",
    "qs": ">=6.16.0"
  }
}
